export default function ({ store, route }) {
  // store.commit('backgroundClasses/setClasses', {
  //   homepage: route.path === '/',
  //   collection: ['/recette', '/couture', '/betise'].indexOf(route.path) > -1,
  //   item: route.name === 'collection-id',
  //   nendoroids: route.name === 'nendoroids',
  //   cartons: route.name.includes('carton')
  // })
}
